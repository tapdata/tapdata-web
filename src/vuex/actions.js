const actions = {
    //     login (context, payload) {
    //       context.commit('login', payload)
    //     },
    //     logout (context) {
    //       context.commit('logout')
    //     }
}
export default actions